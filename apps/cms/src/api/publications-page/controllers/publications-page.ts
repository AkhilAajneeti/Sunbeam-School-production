/** Publications Page — the default core controller, unmodified. */
import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::publications-page.publications-page');
