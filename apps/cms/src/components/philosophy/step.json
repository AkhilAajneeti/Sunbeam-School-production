{
  "collectionName": "components_philosophy_steps",
  "info": {
    "displayName": "Step",
    "icon": "arrowRight",
    "description": "One move in a sequence — a name, a sentence, a photograph and a small mark. ⚠ Its position in the run and the delay it animates on are design: the page spaces them and staggers them from how many there are."
  },
  "options": {},
  "attributes": {
    "label": { "type": "string", "required": true, "maxLength": 60 },
    "body": { "type": "text", "required": true, "maxLength": 1200 },
    "photo": { "type": "media", "multiple": false, "required": true, "allowedTypes": ["images"] },
    "photoAlt": { "type": "text", "required": true, "maxLength": 500 },
    "icon": { "type": "media", "multiple": false, "allowedTypes": ["images"], "description": "The small mark beside the step. Decorative — it carries no description because the photograph already says what the step is." }
  }
}
